/**
 * Internal business modules hosted inside the Nexra modular monolith.
 *
 * <p>Each child package below this root represents one bounded domain and should keep
 * its own controller, service, repository, config, validation, and tests.</p>
 */
package com.nexra.hrms.nexra.modules;

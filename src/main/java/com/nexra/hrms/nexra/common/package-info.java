/**
 * Shared, cross-cutting library used by every Nexra module.
 * This package hosts the canonical API response contract, global exception
 * handler, base auditable entity, security headers customizer, correlation id
 * filter, global rate limit filter, metric logging aspect, and OpenAPI config.
 * Business modules MUST NOT duplicate these types.
 *
 * @author niteshjaitwar
 * @version 1.0
 */
package com.nexra.hrms.nexra.common;

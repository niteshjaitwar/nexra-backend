apiVersion: v1
kind: Service
metadata:
  name: nexra
  namespace: nexra
  labels:
    app: nexra
spec:
  selector:
    app: nexra
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: ClusterIP

